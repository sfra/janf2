The values x=@{x}@ and y=@{y}@ have been send by get<br />The object _GET in the context of the current action 
          has been used. It contains all parameters from the url. They are accessible by the expressions:
            <pre>this._GET['x']</pre>
            However, parameters are sending in the following form:
            <pre>httt://domainame.org/x/valueOfx/y/valueOfy</pre>Change the values in the url of the present page and see the results.